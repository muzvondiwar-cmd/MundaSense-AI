# Evaluation artefacts

The training command writes machine-readable held-out metrics to `metrics/evaluation.json` and
two labelled PNG figures to `figures/`. Every artefact produced by the default path describes
synthetic demonstration data, not field validation.

